# Copy to .env and fill in. .env is git-ignored — never commit real keys.
# You only need ONE provider for the labs. OpenAI is the default in application.yml.

OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional: point at a local model server (Lab 6 — keep data in the building).
# OLLAMA_BASE_URL=http://localhost:11434
