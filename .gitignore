# Build output
/target/**
*/target/**
*/*/target/**
*/*/*/target/**

# IDE
.idea/**
*.iml

# Secrets — NEVER commit real keys. Ship .env.example only.
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.keystore

# Logs
**/*.log

# OS
.DS_Store

# MkDocs build output
site/
.cache/

# Local source materials (The Sovereign Engineer PDF + distilled models) — not for
# distribution; slide specs under slides/ carry the quotes and citations they need.
# Anchored to the repo root so docs/reference/ (the docs-site section) stays tracked.
/reference/

# Note: .claude/ is intentionally tracked — it ships the workshop's build-time
# harness (.claude/agents/java-implementer.md). Do not ignore it.
